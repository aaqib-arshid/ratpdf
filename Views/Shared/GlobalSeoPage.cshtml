@model ratpdf.Models.GlobalSeoPageModel

@{
    ViewData["Title"] = Model.Title;
    ViewData["Description"] = Model.Description;
}

<h1>@Model.Title</h1>

<div>
    @Html.Raw(Model.FullContentHtml)
</div>
